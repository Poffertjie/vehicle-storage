
<VehicleGrid  SaveData="SaveDetails"/>

@code {
    
    [Parameter]
    public EventCallback<string> GetDetails { get; set; }

    private async Task SaveDetails(string details)
    {
        await GetDetails.InvokeAsync(details);
    }
}